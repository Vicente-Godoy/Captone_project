import React from "react";

function Chat() {
  return (
    <div style={styles.container}>
      <h2>Chat</h2>
    </div>
  );
}

const styles = {
  container: {
    textAlign: "center",
    paddingTop: "40px",
  },
};

export default Chat;